13.0.1.1
--Remove error in res.config.setting store

13.0.1.2
--timesheet calculation improved

13.0.1.3
-- ADMIN & USER, when ADMIN tries to see the timer it always starts from 0.0 and when we check from the USER account it already started and shows the correct time.
